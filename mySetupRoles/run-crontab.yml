---
- name: HOSTNAME | Initialisation
  hosts: allservers
  # remote_user: root
  remote_user: ibrahim
  become: true
  become_user: root
  roles:
    - crontab

- name: HOSTNAME | Initialisation - LOCALHOST
  hosts: local
  connection: local
  # remote_user: root
  remote_user: ibrahim
  become: true
  become_user: root
  roles:
    - crontab

  # complete, tested and working
